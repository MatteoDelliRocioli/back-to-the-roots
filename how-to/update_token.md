# How to check the current configured remotes: 
```bash
git remote -v
```

# How to update access token to remote:
```bash
git remote set-url origin https://<token-name>:<token-value>@github.com/<path-to-your-project-root>.git
```
